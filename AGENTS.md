# Agent 开发指南

本文件是开发入口，不是产品说明书或实现规格。当前可用能力看 README 和代码；
长期产品取舍看下列生效 ADR。不要把设计决定写成已完成的功能。

## 工作方式

先核对目标仓库、分支、工作区变化和适用的指令，保留无关及并发改动。
读取与任务有关的代码和测试；只有任务涉及某项取舍时才展开对应 ADR。
如有明确适用的 Skill，先读完整指令；这不意味着产品需要内建 Skill 系统。

普通实现、检查和本地 Git 操作自主完成。提交只包含本次任务的改动。
推送、发布、部署、重启现有服务或不可逆操作，需要本次任务的明确授权。
新项目的开发不授权修改或重启正在使用的 codex-mcp。

README、ADR 等文档、Web UI 和现有运行错误指引继续使用中文。
模型可见的 MCP instructions、工具/资源标题和描述、schema 参数说明以及 ALL_TOOLS 包装使用英语，
以固定 Codex 版本及核对过的上游提示为参考；差异须有实际环境或契约依据，见 ADR-002。
代码标识符、协议字段、命令、枚举和上游原始数据不翻译；用户补充、Skill 正文和第三方契约保持原文。
公开文档不要带入个人路径、公司环境、凭据或内部部署前提。

## 不做 Spec 驱动开发

先读代码，再做最小、完整的改动。能从实现、测试、schema 或依赖锁文件直接得到的信息，
不要再维护一份文档副本。不要为普通功能生成规划 Spec、逐文件施工图或未经用户提出的长期待办清单。

需要留下的是：选择某条路线的原因、被放弃的合理方案、真实边界，以及容易被后续 Agent
“优化掉”的必要知识。每个事实只设一个主位置，其他文档链接过去。

| 文档 | 读者与职责 |
| --- | --- |
| README.md | 用户：产品用途、真实可用性、安装与连接、权限和使用限制 |
| docs/configuration.md、docs/connections.md | 用户按需查阅的配置和连接步骤，不承载另一份架构规格 |
| AGENTS.md | Agent：开发纪律、阅读入口，不复制完整设计 |
| ARCHITECTURE.md | 开发者与 Agent：长期职责边界，不维护文件地图或调用链副本 |
| docs/adr-*.md | Agent 为主：当前生效的选择、理由和代价 |
| [docs/BACKLOG.md](docs/BACKLOG.md) | 用户明确保留、尚未交付的计划与前置条件，不是当前实现要求 |

ADR 正文保持当前有效；同一主题通常原地更新，历史由 Git 保存。
若用新 ADR 替代旧决定，明确标记替代关系并更新本索引，不让两个相反决定同时有效。
实现偏离决定时先判断是缺陷还是产品取舍改变，不以“代码已经这样写了”为理由静默改口。
用户明确改变方向时同步更新相关决定，不沿用旧项目的相反约束。
Backlog 不覆盖生效 ADR，也不授权自动开始实现；计划状态变化时同步更新条目。

产品按可信个人与有能力的 Agent 设计；执行权限、无逐调用审批及环境恢复责任以 ADR-004 为准，
下游启动全量加载而非搜索解锁以 ADR-002 为准。不要为这两项边界另建相反的保护/懒加载层。

## 按任务读取

| 任务 | 生效决定 |
| --- | --- |
| 工具入口、工作目录、补丁、Codex 复用、跨平台 | [ADR-001：执行内核与产品边界](docs/adr-001-exec-runtime.md) |
| ALL_TOOLS、契约发现、英文模型描述、下游 MCP | [ADR-002：工具发现与契约](docs/adr-002-tool-discovery.md) |
| JSON 去重、显式预算、store/load、媒体、cell/进程 | [ADR-003：结果与执行生命周期](docs/adr-003-results-lifecycle.md) |
| Tunnel、认证、安装、发布及机器权限 | [ADR-004：接入与信任边界](docs/adr-004-connectivity-trust.md) |
| 原生附件、文件导入导出、资源读取与下载 URL | [ADR-005：文件传输与交付边界](docs/adr-005-file-transfer.md) |
| Skill 发现、软链接、显式调用策略与目录预算 | [ADR-006：一次发现的 Skill 目录](docs/adr-006-skill-catalog.md) |
| Shell 默认值、单次覆盖及精简动态契约 | [ADR-007：默认命令 Shell 与单次覆盖](docs/adr-007-command-shell.md) |
| 完整环境继承、出站代理与 Node 20 兼容 | [ADR-008：环境、代理与 Node 20](docs/adr-008-environment-and-proxy.md) |
| Web 控制台、局域网认证、审计边界与前端交付 | [ADR-009：可选的本机 Web 管理控制台](docs/adr-009-web-console.md) |
| 会话备注、异步提问、Web 作答与 User Note FIFO | [ADR-010：Web 会话补充与异步提问](docs/adr-010-session-notes.md) |

## 实现与验证

保持一个可执行契约来源，生成需要展示的说明；不要手工同步顶层描述、目录条目和文档 schema。
仅注册 exec/wait 两个顶层工具；本机契约同时生成 exec 的完整工具说明、ALL_TOOLS 条目和 tools.* 绑定。
ALL_TOOLS 保留本次已绑定的全部本机/下游契约；下游按需用 JS 筛选查看，不另建搜索/调用工具或直接入口。
工具说明只保留首次正确调用所需的信息；优先正向表达，历史禁令和内部机制留在 ADR，详见 ADR-002。
用成熟依赖解决已有问题，不为了“全新开发”重写可靠底层；也不整体搬入旧项目的 UI 和历史兼容层。
复用上游代码时保留必要的许可证和归属，不能把参考仓库变成隐式运行依赖。

根据风险做最小充分验证。工具契约要检查真实 MCP 返回、参数校验和陌生 Agent 的可用性，
不能只证明本地函数或 mock 成功。跨平台改动需验证相应平台的真实进程和安装路径；
一次 Linux 通过不等于 Windows/macOS 已支持。
版本变更同步包清单、锁文件和运行时常量；验证 CLI、MCP 和 Web 报告一致。
源码版本、Git tag/Release、npm 发布与部署是不同动作，按本次明确授权操作，发布边界见 ADR-004。

纯文档任务检查事实、链接、决策一致性、措辞和 Git diff，不安装依赖、不跑无关构建。
结束时说明实际改动与验证、Git 状态、未验证边界，以及是否发生外部操作。
