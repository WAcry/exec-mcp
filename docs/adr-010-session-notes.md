# ADR-010：Web 会话补充与异步提问

状态：生效。日期：2026-09-20。

## 两种来源，共用用户补充通道

操作者在 Web 的现有会话组中发送补充，由同一对话下一次工具自然返回时附带。
补充既可由用户主动编写，也可由用户回答 Agent 的异步问题产生；两者共用队列、投影和生命周期。
模型只新增 request_user_input_async，顶层与 exec 内同名调用共用契约；不提供查询回答、写用户消息或命名会话的工具，
不增加确认参数、同步等待用户、浏览器系统通知或已删除的问答数据库。
默认按会话哈希展示，只允许操作者手动设置备注名，不让 ChatGPT 生成名字。备注仅用于 Web 辨认，不参与路由或进入模型消息。

归属沿用 `_meta["openai/session"]` 的 SHA-256 摘要，而非连接、原生 session、cell、终端或目录。
仅从带有此标识的实际工具调用发现收件人；缺失标识的组不能发送。所有顶层工具共用附带逻辑，
成功、运行中及普通执行错误均可携带，嵌套工具原值、tools/list、资源读取与 Web 查询不消费消息。
在返回时选取，因此等待期间提交也有机会搭上当前响应；消息不唤醒长等待、不改写已运行脚本，也不是紧急停止或审批机制。

## 提交问题立即结束，回答才生成用户补充

参考固定 Codex rust-v0.155.1 的
[request_user_input_async](https://github.com/openai/codex/blob/rust-v0.155.1/codex-rs/core/src/tools/handlers/request_user_input_async.rs)，
沿用 questions/title/options（字符串数组）和 accepted 的形状。await 只等待提交，不等待人作答；
与 Codex 宿主新用户消息不同，本项目将回答随下一次普通工具响应送回。Agent 可继续不依赖该答案的工作。

本产品要求每题提供多个选项。首项标记推荐，但不预选、不自动回答；界面自动提供“以上都不是”，
任何选项都允许独立补充，只有自定义选择必须填写文本。问题按当前对话哈希归组，保留用户的手动备注。
全局待答提示只导航到会话组，侧栏将问题和补充记录分开；未答题优先，但所有问题均可分页查看。
每题独立提交。两页同时作答时，先保存者成功，另一份草稿保留；发送不确定时同提交 ID 重试去重。
待投递答复可撤回并重新选择；已附带的答案不能撤销，只能继续补充，不能冒称模型已读。

可选 request_key 只对同一对话的同内容创建去重；同键改题报冲突。服务分配问题与选项位置身份，
Agent 无需手写 ID。回答端只提交选项位置和补充；题目与选项从原记录还原，不接受浏览器改写。
成功作答在同一同步步骤中更新问题并加入现有 User Note FIFO；空间/校验失败时保持待答，不丢正文。
消息内容是“问题＋选择＋补充”，不把全部未选项、请求 ID 或审计时间再次送给模型。
按回答提交时间与主动补充统一排序；尚未回答的问题不占投递队列，也不会挡住其他补充。

提问要求 Web 已实际监听以及宿主对话标识；缺失时明确失败，不退回全局问题箱。
记录与 User Note 共享有界实例内存与空闲历史策略，不修改原生 host，不建设任务系统或自动续跑机制。
问题、选择和补充的实际编码也必须可装入一条补充；提交前校验，避免保存一个永远无法独立投递的回答。

## 补充与结果使用同一文本通道

若正常结果含 structuredContent，附带消息时将它包装为 `{result: 原结构化值, user_notes: [补充原文]}`。
这样只读取结构化结果的客户端也能同时取得用户补充；不用把无关字段混入工具原值，避免覆盖同名字段。
原有独立 TextContent 放入可选的 result_content，保留块元数据；仅去除可证明等价的 JSON 文本镜像。
图片、音频、原生资源仍在 content，不改成 JSON/Base64 文本。isError 和其他响应元数据保持不变。
若正常结果没有 structuredContent（包括大结果已退化为文本），仍只在 content 追加「用户额外补充：」和正文。
没有实际附带消息时完全保持原结果；exec 内的工具原值不受此包装影响。直接边界没有强制 outputSchema。

官方文档将 content 与 structuredContent 均列为模型可见字段；这里不认定 ChatGPT 必然隐藏其中一项，
而是避免消费方只选结构化结果时遗漏补充。该投影不能保证模型一定理解或遵循补充。
消息按数组/内容块顺序提供；编号、ID、时间和备注只留在内部队列与 Web 审计中，不再占模型消息。

## 只使用剩余额度

普通工具结果保持原有 36,000 UTF-8 字节保护；有补充时合计最多 37,000 字节。
按最终承载形式计费，包括结构化包装、JSON 字符串转义、数组分隔符或文本标题，再按 FIFO 加入完整消息。
不为消息重新裁剪正常结果，不拆分或截断消息，不跳过放不下的队首。队首一直放不下就继续排队，这是有意接受的简单取舍。
不宣称这些字节值等于 ChatGPT 永久固定的 token 上限。媒体与原生文件保留，真实编码载荷上限仍需独立检查。

选取、检查与标记附带在同步步骤中完成，避免并发响应重复消费；同一批按序排列，跨响应抵达顺序不作网络保证。
附带前已取消或编码失败则保持待发。附带后不重发，也不要求模型确认；UI 写“已附入工具响应”，不能称已读或已执行。
连接器途中丢失响应可能丢失本次投递，用户保留复制入口；不为这项低频补充引入确认协议。

## 生命周期与 Web 边界

问题、消息及备注保存在独立的有界实例内存中，不随审计滚动、清空历史或原生 host 回收删除。
执行服务重启复用它；整个程序退出后不恢复。按需清理 72 小时旧记录，计费容量约 16 MiB，满时拒绝新提交而不覆盖待发消息。
正文按 30,000 UTF-8 字节校验，保留原文；浏览器用提交 ID 去重，内容变化作为新消息。仅待发消息可撤回。
实际 Web 监听期间发现新的可用收件人；关闭 Web 不删除既有消息，原对话后续调用仍可携带。

沿用 Web 同源、鉴权和写请求标记；SSE 只发变化通知与会话摘要，不广播消息正文。
草稿按收件人隔离，提交成功后才清空对应草稿；备注和正文按纯文本渲染。
这是可信单操作者的沟通通道，不以同账户进程可访问的 Web API 冒充只有真人能授权的安全边界。

依据：[OpenAI 对话元数据和可见工具结果](https://developers.openai.com/plugins/reference)、
[MCP 内容块](https://modelcontextprotocol.io/specification/2025-11-25/server/tools)。
